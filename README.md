Контейнеры разворачиваются командой docker-compose up -d --build --scale app=2
Если приложение не поднялось в контенере, необходимо остановить контейнеры командой "docker-compose down" и развернуть их еще раз.
